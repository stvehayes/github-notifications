import React, { HTMLAttributes } from 'react';
import { IconProps } from '@primer/octicons-react';
import { SxProp } from '../sx';
export declare type SegmentedControlButtonProps = {
    children?: string;
    /** Whether the segment is selected */
    selected?: boolean;
    /** The leading icon comes before item label */
    leadingIcon?: React.FunctionComponent<IconProps>;
} & SxProp & HTMLAttributes<HTMLButtonElement>;
declare const SegmentedControlButton: React.FC<SegmentedControlButtonProps>;
export default SegmentedControlButton;
